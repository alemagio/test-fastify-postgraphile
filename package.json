{
  "name": "test-fastify-postgraphile",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "tap test/**/*.test.js",
    "start": "docker-compose up -d && fastify start -l info app.js",
    "dev": "docker-compose up -d && fastify start -w -l info -P app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fastify": "^3.0.0",
    "fastify-autoload": "^3.0.2",
    "fastify-cli": "^2.3.0",
    "fastify-plugin": "^2.3.0",
    "middie": "^5.1.0"
  },
  "devDependencies": {
    "tap": "^14.0.0"
  }
}
